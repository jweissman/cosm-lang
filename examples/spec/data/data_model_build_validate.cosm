let Ticket = Data.model("Ticket", {
  title: Data.string(),
  priority: Data.enum("low", "high")
}, {
  priority: "low"
})

let built = Ticket.build({ title: "Demo" })
let invalid = Kernel.tryValidate({ title: "Demo", priority: "urgent" }, Ticket)
[built.title, built.priority, invalid.ok]
