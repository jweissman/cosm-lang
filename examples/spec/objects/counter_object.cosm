class Counter
  def init(value) @value = value end

  def current = @value

  def increment = Counter.new(@value + 1)
end

let counter = Counter.new(2)
[counter.current(), counter.increment().current()]
