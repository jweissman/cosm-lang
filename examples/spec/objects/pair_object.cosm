class Pair
  def init(left, right)
    @left = left
    @right = right
  end

  def sum = @left + @right

  def inspect = "<Pair " + @left.to_s() + ", " + @right.to_s() + ">"
end

let pair = Pair.new(1, 2)
[pair.sum(), pair.inspect()]
