module Labelable
  def label = "label:" + self.class.name
end

module LoudLabel
  def loud_label = "[" + label() + "]"
end

class Mixed
  def describe = loud_label() + "!"
end

Mixed.include(Labelable)
Mixed.include(LoudLabel)
Mixed.new().describe()
