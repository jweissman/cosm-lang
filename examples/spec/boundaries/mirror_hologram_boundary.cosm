require "cosm/hologram"

let proc = Cosm::Hologram.wrap(Process)
proc.set(:label, "runtime")
[Mirror.status().mode, Cosm::Hologram.status().intended_role, proc.get(:platform).call(), proc.get(:label)]
