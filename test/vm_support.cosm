let transcript = ["user: hello"]
let reply = if transcript.length == 1 then
  { text: Kernel.dispatch("Try the Reset Session button.", :plus, ""), ok: true }
else
  { text: "Unexpected state", ok: false }
end
reply.text
